import 'package:flutter/material.dart';

class PersonsLocation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Persons location'),
      ),
      body: Center(
        child: Text('Person 1')
      ),
    );
  }
}
